# Bot Engine v2.1 — Environment Variables
# Copy to .env and set your values

# HMAC secret for challenge signing (generate with: python3 -c "import secrets;print(secrets.token_hex(32))")
BOT_HMAC_SECRET=change_me_in_production

# Dashboard credentials
DASHBOARD_USER=admin
DASHBOARD_PASS=changeme
