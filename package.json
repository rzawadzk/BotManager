{
  "name": "bot-engine-js",
  "version": "2.1.0",
  "private": true,
  "description": "Client-side JavaScript for the Bot Engine (PoW challenge, captcha, canary). Minified with terser at build time and embedded into HTML by the Python server. See static/js/README.md.",
  "scripts": {
    "build": "bash tools/build_js.sh",
    "build:check": "bash tools/build_js.sh --check",
    "clean": "rm -rf static/js/dist"
  },
  "devDependencies": {
    "terser": "^5.24.0"
  }
}
